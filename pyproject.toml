[project]
name = "pacman"
version = "0.0.1"
description = "Ghosts! More ghosts!"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "flake8",
    "mypy",
]

[tool.mypy]
explicit_package_bases = true
